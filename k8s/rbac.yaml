apiVersion: v1
kind: ServiceAccount
metadata:
  name: bookstore-sa
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: bookstore-role
  namespace: default
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "endpoints"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "watch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: bookstore-rolebinding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: bookstore-sa
    namespace: default
roleRef:
  kind: Role
  name: bookstore-role
  apiGroup: rbac.authorization.k8s.io
